version: '3.8'

services:
  web:
    image: myapp
    ports:
      - "5001:80"
    depends_on:
      - db
    environment:
      - ConnectionStrings__DefaultConnection=Server=db;Database=SilverOakGateClubDb;User Id=sa;Password=rudr12@34;TrustServerCertificate=True;

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      SA_PASSWORD: "rudr12@34"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"